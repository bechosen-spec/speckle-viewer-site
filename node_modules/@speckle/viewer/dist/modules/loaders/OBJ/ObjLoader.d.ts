import { Loader } from '../Loader.js';
import { WorldTree } from '../../../index.js';
export declare class ObjLoader extends Loader {
    private baseLoader;
    private converter;
    private tree;
    private isFinished;
    get resource(): string;
    get finished(): boolean;
    constructor(targetTree: WorldTree, resource: string, resourceData?: string);
    load(): Promise<boolean>;
    cancel(): void;
    dispose(): void;
}
