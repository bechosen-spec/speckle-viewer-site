import { Loader } from '../Loader.js';
import { WorldTree } from '../../../index.js';
export declare class SpeckleLoader extends Loader {
    private loader;
    private converter;
    private tree;
    private isCancelled;
    private isFinished;
    get resource(): string;
    get finished(): boolean;
    constructor(targetTree: WorldTree, resource: string, authToken?: string, enableCaching?: boolean, resourceData?: string | ArrayBuffer);
    load(): Promise<boolean>;
    cancel(): void;
    dispose(): void;
}
