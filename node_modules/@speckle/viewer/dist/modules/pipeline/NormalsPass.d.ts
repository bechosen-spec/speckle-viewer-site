import { Material, OrthographicCamera, PerspectiveCamera, Plane, Scene, Texture, WebGLRenderer } from 'three';
import { BaseSpecklePass, type SpecklePass } from './SpecklePass.js';
export declare class NormalsPass extends BaseSpecklePass implements SpecklePass {
    private renderTarget;
    private normalsMaterial;
    private scene;
    private camera;
    private colorBuffer;
    onBeforeRender: (() => void) | undefined;
    onAfterRender: (() => void) | undefined;
    get displayName(): string;
    get outputTexture(): Texture;
    get material(): Material;
    constructor();
    setClippingPlanes(planes: Plane[]): void;
    update(scene: Scene, camera: PerspectiveCamera | OrthographicCamera): void;
    render(renderer: WebGLRenderer): void;
    setSize(width: number, height: number): void;
}
