import { OrthographicCamera, PerspectiveCamera, Scene, Texture, WebGLRenderTarget, WebGLRenderer } from 'three';
import { BaseSpecklePass, type SpecklePass } from './SpecklePass.js';
export declare class ColorPass extends BaseSpecklePass implements SpecklePass {
    private camera;
    private scene;
    private overrideMaterial;
    private _oldClearColor;
    private clearColor;
    private clearAlpha;
    private clearDepth;
    onBeforeRenderOpauqe: (() => void) | undefined;
    onAfterRenderOpaque: (() => void) | undefined;
    onBeforeRenderTransparent: (() => void) | undefined;
    onAfterRenderTransparent: (() => void) | undefined;
    constructor();
    get displayName(): string;
    get outputTexture(): Texture | null;
    update(scene: Scene, camera: PerspectiveCamera | OrthographicCamera): void;
    render(renderer: WebGLRenderer, _writeBuffer: WebGLRenderTarget, readBuffer: WebGLRenderTarget): void;
}
